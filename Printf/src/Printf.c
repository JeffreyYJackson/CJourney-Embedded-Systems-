#include <stdio.h>

int main(){

	int grade = 100;
	char subject[] = "math";
	printf("I got a %d in my %s exam\n", grade, subject);



	/*
		You can use format specifiers to format you strings inside of your "printf" function.
		More than one specifiers can be used in a printf function. The order of the paramaters should be the same as the specifiers.


		%d > integers  ("%i" can be used instead of "%d" but it is not advised)
		%s > string  


	*/


}